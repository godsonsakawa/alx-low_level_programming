#include "main.h"
/**
 * _strlen - counts the length of a string.
 * @s: references the char to be counted.
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
